describe "Agregando falhas em matchers de comparação" do

  # it 'Aggregate_failures first syntax' do
  #   aggregate_failures do
  #     expect(5).to be_between(2,7).inclusive
  #     expect(1).to be_between(2,7).inclusive
  #     expect(8).to be_between(2,7).inclusive
  #   end
  # end

  # it 'Aggregate_failures second syntax', :aggregate_failures do
  #   expect(5).to be_between(2,7).inclusive
  #   expect(1).to be_between(2,7).inclusive
  #   expect(8).to be_between(2,7).inclusive
  # end
end
